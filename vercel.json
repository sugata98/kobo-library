{
  "buildCommand": "cd library-ui && npm install && npm run build",
  "outputDirectory": "library-ui/.next",
  "devCommand": "cd library-ui && npm run dev",
  "installCommand": "cd library-ui && npm install",
  "framework": "nextjs",
  "rootDirectory": "library-ui"
}

